---
description: 查询 @kne / remote-loader 组件文档时，用 @kne/npm-tools 拉取并索引到 ~/.kne_document_indexed
alwaysApply: true
---

# KNE 文档搜索（@kne/npm-tools）

涉及 `@kne/*` 包 API/用法，或 `remote-loader` 远程组件（如 `components-core:FormInfo`）时，**禁止凭记忆编造 API**。必须用 `@kne/npm-tools` 获取 README、切分索引后再回答。

索引根目录：环境变量 `KNE_DOCUMENT_INDEXED_DIR`，默认 `~/.kne_document_indexed`。

## 何时触发

- 使用 / 查阅 `@kne/...` 依赖
- 使用 / 查阅 `src/preset.js` 里 `remoteLoaderPreset` 的 remote 组件
- 用户问某组件 props、示例、目录结构

## 强制工具（禁止手写切分）

通过 Node 调用（优先当前项目已装的 `@kne/npm-tools`；否则 `npx` / 本机已有安装路径）：

```js
const {
  getRemoteModuleDocument,
  getNpmPackageDocument,
  buildDocumentIndex,
  getDocumentIndexDir
} = require('@kne/npm-tools');
```

| API | 用途 |
|-----|------|
| `getRemoteModuleDocument(remote)` | remote-loader 参数 → 解析部署地址并拉取 README |
| `getNpmPackageDocument(name)` | npm 包 README（`@kne/load-npm-info`） |
| `buildDocumentIndex({ id, version, readme, ... })` | DOC_MD 切分 + 落盘 `meta/index/components.json` |
| `getDocumentIndexDir()` | 解析/创建索引根目录 |

**禁止**自行 `extractDocMd` / 手写 `# ` 切分 / 直接 `require('@kne/md-doc').parse` 替代上述流程（除非 npm-tools 不可用且用户允许临时兜底）。

## 流程

1. **先查索引**：`<root>/<id>/<version>/` 下已有非空 `index.json` + `components.json` → 直接读，不重复拉取
2. **缺失则拉取 + 建索引**
3. **再按组件 / 关键字查询**作答

### 远程组件

1. 从 `src/preset.js`（或等价）取出 remotes 项：`{ url, tpl, remote, defaultVersion }`
2. 调用：

```js
const doc = await getRemoteModuleDocument({
  url, tpl, remote, defaultVersion // 或 version
});
await buildDocumentIndex({
  id: doc.remote,
  version: doc.version,
  readme: doc.readme,
  readmeUrl: doc.readmeUrl,
  source: 'remote'
});
```

开发态 `url: '/'` 且本地即该 remote 源码时：可读仓库根 `README.md`，再 `buildDocumentIndex`。

### `@kne/` npm 包

```js
const doc = await getNpmPackageDocument('@kne/xxx'); // 可带 @version
await buildDocumentIndex({
  id: doc.packageName,
  version: doc.version,
  packageName: doc.packageName,
  readme: doc.readme,
  source: 'npm'
});
```

本地已装包也可先读 `node_modules` 下 README，再交给 `buildDocumentIndex`（切分仍必须走 npm-tools）。

## 索引结构

```
~/.kne_document_indexed/<id>/<version>/
  meta.json
  index.json        # [{ name, token, summary }]
  components.json   # { [name]: { name, token, summary, api, examples } }
```

- 远程：`<id>` = remote 名（如 `components-core`）
- npm：`<id>` = 包名（如 `@kne/react-fetch`）
- `token`：`{id}:{componentName}`（如 `components-core:FormInfo`）

## 查询

| 意图 | 做法 |
|------|------|
| 组件列表 | 读 `index.json` |
| API / 概述 | `components.json[name].api` / `.summary` |
| 示例 | `components.json[name].examples` |
| 关键字 | `rg -i "<keyword>" ~/.kne_document_indexed`（可 `--glob '*.json'`） |

回答优先引用索引中的 **api / examples.code**，并注明 `token` 与版本。

## 禁止

- 未查索引就编造 props / 事件 / 示例
- 索引缺失时不调用 npm-tools 拉取/建索引就猜测
- 整份巨型 README 无切分塞进上下文
- 绕过 `@kne/npm-tools` 自造索引格式
