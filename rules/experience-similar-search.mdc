---
description: 动手前在 ~/.kne_document/experience 分层检索相似经验，省 token
alwaysApply: true
---

# experience 相似问题检索（读取）

本规则独立于开发流程、问题解决总结写入与 KNE 文档搜索。只负责动手前**读取** `~/.kne_document/experience`。

开始改代码、排错，或用户问「怎么做」且可能有历史经验时先查。无目录 / 无命中 → 一句话带过，直接继续，不空转。写入规范见 `experience-summary-write`。

路径：`~/.kne_document/experience/{projectName}/{YYYY-MM-DD-HH-mm-ss}/{title}.json`

## 原则

**先窄后宽、先路径后内容、先摘要后全文。** 禁止把整库 JSON 塞进上下文。

## 检索阶梯（必须按序）

1. **抽词**：从当前需求抽 **1–3** 个实词（组件名 / 动作 / 目录名），勿用长句当 query。
2. **同项目优先**：已知仓库名则先搜 `~/.kne_document/experience/<projectName>/`；无命中再扫其他项目（仍限候选数）。
3. **文件名零解析**：末段文件名即 title。用 `find … -iname '*关键词*'` / `ls` 粗筛，**不读文件内容**。
4. **浅内容**：对候选 `rg -n -i '关键词' --glob '*.json'`，**只看匹配行**；优先含 `"title"` / `"description"` / `"summary"` 的行。禁止把整文件输出进对话。
5. **摘要投影**（每条最多一次 `jq`）：只取
   - `title`, `description`, `score.overall`
   - `finalSolution.summary`, `finalSolution.keyChanges`
   - **默认不取** `proposals`、`requirement.detail`、`codeSnippets`
6. **深读限额**：决定复用时，**仅对 1 条**再取 `finalSolution.codeSnippets`（或该文件全文）。
7. **硬上限**：候选 ≤ **3**；`score.overall < 6` 默认丢弃（除非没有更高分命中）。

## 对用户输出

最多 1–3 条：`title` + `overall` + 一句话（`description` 或 `finalSolution.summary`）+ 文件路径。命中且沿用时先复述摘要再动手。

## 禁止

- `find ~/.kne_document/experience | xargs cat`（或等价整库读取）
- 一次性 Read / 粘贴多个完整 JSON
- 把历史 `proposals` 全列表贴进上下文「供参考」
- 无命中后反复扩大检索空转
